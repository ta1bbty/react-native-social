declare type PropsType = {
    numberOfBullets: number | null;
    itemIndexCurrentlyFocused: number;
    darkMode: boolean;
    id: string;
};
export declare const InteractionRow: (props: PropsType) => JSX.Element;
export {};
